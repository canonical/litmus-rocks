name: Update rock

on:
  workflow_dispatch: {}
  schedule:
    - cron: '0 0 * * *'

jobs:
  update:
    name: Update rock
    strategy:
      fail-fast: false
      matrix:
        rock-path:
          - chaos-exporter
    uses: canonical/observability/.github/workflows/rock-update@feat/support-rock-monorepos
    with:
      rock-path: ${{ matrix.rock-path }}
